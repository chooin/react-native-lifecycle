{
  "name": "react-native-lifecycle",
  "private": true,
  "scripts": {
    "start": "pnpm dev",
    "dev": "pnpm dev --filter example",
    "build": "pnpm build --filter react-native-lifecycle",
    "test": "pnpm test --filter react-native-lifecycle",
    "preinstall": "npx only-allow pnpm",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{js,json}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "@types/node": "^18.15.11",
    "lint-staged": "^13.2.1",
    "prettier": "^2.8.7"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1"
  },
  "engines": {
    "node": "18.x"
  }
}
